class PerfilRestaurante {
    titulo: string
    image: string
    etiqueta: string

    constructor(
        titulo: string,
        image: string,
        etiqueta: string
    ) {
        this.titulo = titulo
        this.image = image
        this.etiqueta = etiqueta
    }
}

export default PerfilRestaurante